[project]
name = "protein-interpretability"
version = "0.1.0"
description = "Experiments in protein language models and interpretability"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marimo>=0.10.0",
    "numba>=0.66",
    "numpy>=2.0",
    "plotly>=6.0",
    "umap-learn>=0.5.6",
]
